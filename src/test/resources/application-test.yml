spring:
  datasource:
    url: jdbc:h2:mem:test_db;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false

app:
  security:
    jwt:
      secret: "404bead1a00c663e8d07a62061a85d5a55b30bffa13f68944612c994e8cab848"
      expiration_minutes: 120
      refresh_expiration_days: 30

logging:
  level:
    com.christn.salesinventoryapi: DEBUG